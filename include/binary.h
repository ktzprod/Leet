#pragma once

#include "config.h"

namespace leet {

    /**
     * @brief sum the two given integer without using the operator +
     *
     * @param a
     * @param b
     *
     * @return a+b
     */
    LEET_API int binary_sum(int a, int b);
}
